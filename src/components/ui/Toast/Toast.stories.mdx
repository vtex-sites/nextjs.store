import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { TokenTable, TokenRow } from 'src/../.storybook/components'

import UIProvider, { useUI } from 'src/sdk/ui/Provider'

import Toast from '.'
import Button from 'src/components/ui/Button'

<Meta title="Atoms/Toast ⚠️" component={Toast} />

export const Template = () => {
  const { pushToast } = useUI()
  return (
    <Button
      variant="tertiary"
      onClick={() => {
        pushToast({
          title: 'Your cart was updated!',
          message: 'Some additional info here.',
          status: 'INFO',
          icon: 'ArrowsClockwise',
        })
      }}
    >
      Display Toast
    </Button>
  )
}

export const TemplateWithProvider = () => {
  return (
    <UIProvider>
      <Toast />
      <Template />
    </UIProvider>
  )
}

<header>

# Toast

Toasts are used to display notifications. They appear temporarily, and should contain a single line of text directly related to the operation performed.

</header>

---

## Usage

`import Toast from 'src/components/ui/Toast'`

<Canvas>
  <Story name="default">{TemplateWithProvider.bind({})}</Story>
</Canvas>

<ArgsTable story="default" />

---
