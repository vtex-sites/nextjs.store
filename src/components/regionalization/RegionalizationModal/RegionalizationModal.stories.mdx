import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { SessionProvider } from '@faststore/sdk'

import { RegionalizationModalContent } from '.'
import {
  TokenTable,
  TokenRow,
  TokenDivider,
} from 'src/../.storybook/components'

<Meta
  title="Atoms/RegionalizationModalContent"
  component={RegionalizationModalContent}
/>

export const Template = (args) => (
  <SessionProvider
    initialState={{}}
  >
    <RegionalizationModalContent {...args} />
  </SessionProvider>
)

<Canvas>
  <Story
    name="overview-default-1"
  >
    {Template.bind({})}
  </Story>
</Canvas>
